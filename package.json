{
    "name": "prana-market-monorepo",
    "private": true,
    "scripts": {
        "setup": "npm install --prefix backend",
        "build": "npm run build --prefix backend",
        "start": "npm run start --prefix backend",
        "strapi": "npm run strapi --prefix backend"
    },
    "engines": {
        "node": ">=18.0.0 <=22.x.x",
        "npm": ">=6.0.0"
    }
}